# 📝 Daily Study Log: 2026-03-17

## 1. 오늘 진행한 내용
* **대상 문제:** 백준 1920번 (수 찾기)
* **사용 언어:** Java
* **핵심 알고리즘:** 이분 탐색 (Binary Search)
* **학습 성과:**
    * 탐색 효율성을 $O(N^2)$에서 $O((N+M) \log N)$으로 개선하는 방법 습득.
    * `BufferedReader`와 `StringTokenizer`를 조합한 Java의 고성능 입출력 패턴 적용.
    * `StringBuilder`를 사용하여 대량의 출력 데이터(10만 건 이상) 처리 시 발생하는 병목 현상 해결.

---

## 2. 다시 공부해야 할 부분 (Deep Dive)
- [X] **Java 입출력 메커니즘:** `br.readLine()` 호출 시 스트림의 포인터 이동과 `StringTokenizer` 객체 재생성 필요성 이해.
- [ ] **이분 탐색의 정밀도:** `mid` 계산 시 정수 오버플로우 방지 로직(`left + (right - left) / 2`) 습관화.
- [ ] **데이터 정렬의 비용:** `Arrays.sort()`의 내부 알고리즘(Dual-Pivot Quicksort)과 그에 따른 시간 복잡도($O(N \log N)$) 리마인드.

---

## 3. 멘토의 냉정한 평가
> **"기초적인 입출력 실수는 실전에서 치명적이다."**

* **강점:** 문제 해결을 위한 메소드 분리(Refactoring) 능력과 `StringBuilder` 활용 등의 클린 코드 감각이 좋음.
* **약점:** Java의 표준 라이브러리(Standard Library) 동작 원리에 대한 이해도가 실제 연차(3년차) 대비 다소 불안정함. 로직이 완벽해도 입력부에서 런타임 에러가 나면 비즈니스 로직은 시작도 못 한다는 점을 명심할 것.

---

## 4. 내일의 성장 주제: 매개 변수 탐색 (Parametric Search)
오늘 배운 '이분 탐색'은 특정 값을 찾는 과정이었습니다. 내일은 이를 응용하여 **"조건을 만족하는 최댓값/최솟값"**을 찾는 방식을 배울 것입니다.

* **추천 문제:** [백준 2805번 - 나무 자르기](https://www.acmicpc.net/problem/2805)
* **핵심 키워드:** `Upper Bound`, `Lower Bound`, 결정 문제(Decision Problem)를 최적화 문제로 바꾸기.

---

**오늘의 한 줄 요약**
"알고리즘은 논리(Logic)이고, 입출력은 기본기(Base)다. 기본기가 흔들리면 논리는 빛을 발할 수 없다."
