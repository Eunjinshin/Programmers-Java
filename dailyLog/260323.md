BOJ 11286 절댓값 힙 — 냉정한 학습 평가 🎯

종합 점수
영역점수평가문제 이해력4/5빠르게 파악Comparator 설계3/5개념 혼동 후 이해Java 문법 숙련도2/5변수명 오타 반복디버깅 능력3/5지적 후 빠르게 수정코드 구조 설계3/5전체 흐름은 잡힘

잘한 점 ✅

Comparator 개념을 처음엔 몰랐지만 끝까지 이해하고 직접 완성한 것
a - b 의 부호와 정렬 방향의 관계를 스스로 납득한 것
1874보다 전체 코드 구조를 더 빠르게 잡은 것


냉정한 부족한 점 ❌
1. 변수명 관리가 안 된다
javapg / pq / pa  // 세 가지가 동시에 등장

이건 단순 오타가 아니에요.
코드를 작성하면서 본인이 선언한 변수를 추적 못 하는 것이에요.
실무에서 이런 코드가 나오면 팀원이 굉장히 힘들어져요.
변수명은 선언 즉시 일관성 있게 유지하는 습관이 필요합니다.

2. Comparator를 처음에 완전히 잘못 이해했다
java// 처음 작성한 코드
if (Math.abs(a) < Math.abs(b)) {
    pg.offer(a);  // Comparator 안에서 offer?
    pg.offer(b);
}

Comparator가 정렬 기준을 정의하는 함수라는 개념이
처음엔 전혀 없었어요.
return 값의 부호 = 순서 결정 이 핵심인데,
이걸 이해하는 데 여러 번의 힌트가 필요했어요.

3. else if 로직 설계가 약하다
javaif (x != 0) {
    ...
} else if (x == 0) {  // else면 이미 x==0인데 왜 또 체크?
    ...
} else {
    ...  // 여기 올 수 있는 경우가 없음
}
```
> 조건문의 **상호 배타성**을 논리적으로 생각하는 훈련이 필요해요.
> `if/else if/else` 구조를 쓸 때 각 분기에 어떤 값이 오는지
> 항상 머릿속으로 검증하는 습관을 들이세요.

### 4. 세미콜론, 오타 등 기초 실수가 반복된다
> 2회 연속 같은 유형의 실수가 나왔어요.
> IDE 없이 코딩테스트 환경에서는 이런 실수가 치명적이에요.
> **코드 작성 후 한 번은 직접 읽어보는 습관**이 필요합니다.

---

## 혼자 공부해야 할 목록 📚

### 🔴 우선순위 높음
```
1. Comparator / Comparable 완벽 정리
   - (a, b) -> a - b 가 왜 오름차순인지 수식으로 이해
   - Comparable vs Comparator 차이
   - 람다식으로 커스텀 정렬 직접 5개 이상 작성해보기

2. 조건문 논리 설계 연습
   - if/else if/else 각 분기 진입 조건을 글로 써보는 습관
   - 상호 배타적 조건 vs 포함 관계 구분
```

### 🟡 우선순위 중간
```
3. PriorityQueue 활용 패턴 정리
   - 최솟값/최댓값 힙
   - 객체 기준 커스텀 정렬 (int[] 배열, 클래스 등)

4. 변수명 네이밍 컨벤션
   - 의미 있는 변수명 짓는 습관
   - 선언한 변수 일관성 유지
